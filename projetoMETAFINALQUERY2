-- admin

CREATE TABLE IF NOT EXISTS log_admin_creation (
    logid SERIAL PRIMARY KEY,
    admin_criado_id BIGINT,
    data_criacao TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- funcao e trigger
CREATE OR REPLACE FUNCTION audit_admin_creation()
RETURNS TRIGGER AS $$
BEGIN
    INSERT INTO log_admin_creation (admin_criado_id)
    VALUES (NEW.utilizador_utilizadorid);

    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS trg_audit_admin ON admin;

CREATE TRIGGER trg_audit_admin
AFTER INSERT ON admin
FOR EACH ROW
EXECUTE FUNCTION audit_admin_creation();


--IDs automaticos

ALTER TABLE utilizador
ALTER COLUMN utilizadorid
ADD GENERATED BY DEFAULT AS IDENTITY;

ALTER TABLE admin
ALTER COLUMN numerofuncionario
ADD GENERATED BY DEFAULT AS IDENTITY;

ALTER TABLE transacao
ALTER COLUMN transacaoid
ADD GENERATED BY DEFAULT AS IDENTITY;

ALTER TABLE bilhete
ALTER COLUMN bilheteid
ADD GENERATED BY DEFAULT AS IDENTITY;

ALTER TABLE aviso
ALTER COLUMN avisoid
ADD GENERATED BY DEFAULT AS IDENTITY;

ALTER TABLE validacao
ALTER COLUMN validacaoid
ADD GENERATED BY DEFAULT AS IDENTITY;

ALTER TABLE compra
ALTER COLUMN compraid
ADD GENERATED BY DEFAULT AS IDENTITY;

ALTER TABLE promocao
ALTER COLUMN promocaoid
ADD GENERATED BY DEFAULT AS IDENTITY;

ALTER TABLE preco
ALTER COLUMN precoid
ADD GENERATED BY DEFAULT AS IDENTITY;

-- Super Admin

INSERT INTO utilizador (nome, email, palavrapasse)
VALUES ('Rafael', 'rafael@mail.com', '1234')
ON CONFLICT (email) DO NOTHING;

INSERT INTO admin (
    super,
    numerofuncionario,
    utilizador_utilizadorid
)
VALUES (
    TRUE,
    1000,
    1
)
ON CONFLICT DO NOTHING;

-- linhas

INSERT INTO linha (
    linhaid,
    nome,
    frequenciamin,
    aberta,
    horaabertura,
    horafechar
)
VALUES (
    1,
    'Linha Amarela',
    15,
    TRUE,
    '2026-05-13 06:00:00',
    '2026-05-13 23:50:00'
)
ON CONFLICT (linhaid) DO NOTHING;

-- tipos de bilhete 

INSERT INTO tipobilhete (
    tipobilheteid,
    nome,
    categoria
)
VALUES
(1, 'Single Trip', 'single'),
(2, 'Daily Pass', 'daily'),
(3, 'Monthly Pass', 'monthly')
ON CONFLICT DO NOTHING;

-- precos

INSERT INTO preco (
    precoid,
    preco,
    datainicio,
    datafim,
    tipobilhete_tipobilheteid
)
VALUES
(1, 1.50, CURRENT_DATE, '2030-01-01', 1),
(2, 5.00, CURRENT_DATE, '2030-01-01', 2),
(3, 30.00, CURRENT_DATE, '2030-01-01', 3)
ON CONFLICT DO NOTHING;

--saldo teste 
UPDATE cliente
SET saldo = 100;

-- verificacoes
SELECT * FROM log_admin_creation;
SELECT * FROM utilizador;
SELECT * FROM admin;
SELECT * FROM cliente;
SELECT * FROM compra;
SELECT * FROM bilhete;
SELECT * FROM transacao;
SELECT * FROM preco;
SELECT * FROM tipobilhete;
